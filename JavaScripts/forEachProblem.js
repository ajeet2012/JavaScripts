// for a given array of numbers, print the value of each value using the forEach loop

console.log("for a given array of numbers, print the value of each value using the forEach loop");


let numberArr = [12, 3, 44, 5, 89, 9];

numberArr.forEach((val, index) => {

    console.log(val * val);

});
